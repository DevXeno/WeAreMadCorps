<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:ff="using:FFImageLoading"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
               Title="Les Equipes"
             x:Class="WeAreMadCorps.Pages.LesEquipes.TeamView">

  <AbsoluteLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" BackgroundColor="Green">
    <ListView
           x:Name="LVMain"
           ItemsSource="{Binding Teams}"
           AbsoluteLayout.LayoutFlags="All"
           AbsoluteLayout.LayoutBounds="0,0,1,1">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <Grid>
              <ffimageloading:CachedImage Source = "{Binding ImagePath}" Aspect="Fill" Opacity="0.5"/>
              <StackLayout>
                <Grid Padding="16" ColumnSpacing="16" BackgroundColor="Transparent">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                  </Grid.ColumnDefinitions>
                  <StackLayout VerticalOptions="Center" Grid.Column="1" Spacing="0" StyleId="SponsorCell">
                    <Label TextColor="Black"  Text="{Binding NameToUpper}"/>
                    <Label Text="{Binding Jeu}"/>
                  </StackLayout>
                </Grid>
              </StackLayout>
            </Grid>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

  </AbsoluteLayout>
</ContentPage>